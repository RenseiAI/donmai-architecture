# BOUNDARY-SYNC integrity check.
#
# Verifies that every BOUNDARY-SYNC-START / BOUNDARY-SYNC-END marker pair in
# this repo's *.md files is byte-identical to its mirrored pair in the sibling
# rensei-architecture repo. See BOUNDARY.md § "Synchronized-section CI hook"
# for the contract.
#
# STATUS: ACTIVE — enabled in Wave 11 after the combined Wave-10+11 push.
# Runs on every PR and main-branch push that touches markdown.

name: BOUNDARY-SYNC integrity

on:
  pull_request:
    paths:
      - '**/*.md'
      - 'scripts/check-boundary-sync*.sh'
      - '.github/workflows/boundary-sync.yml'
  push:
    branches:
      - main
    paths:
      - '**/*.md'
      - 'scripts/check-boundary-sync*.sh'
      - '.github/workflows/boundary-sync.yml'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check out this repo
        uses: actions/checkout@v7
        with:
          path: donmai-architecture

      - name: Check out sibling rensei-architecture
        uses: actions/checkout@v7
        with:
          repository: RenseiAI/rensei-architecture
          path: rensei-architecture
          token: ${{ secrets.BOUNDARY_SYNC_TOKEN }}

      - name: Self-test the checker
        working-directory: donmai-architecture
        run: ./scripts/check-boundary-sync-selftest.sh

      - name: Run BOUNDARY-SYNC check
        working-directory: donmai-architecture
        env:
          DONMAI_ARCH_PATH: ${{ github.workspace }}/rensei-architecture
        run: ./scripts/check-boundary-sync.sh
